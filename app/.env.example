VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Ben dashboard (second Vercel project). Must be a custom JWT with role:"ben_viewer",
# NOT the standard anon key. Generate with: node scripts/gen_ben_jwt.mjs <JWT_SECRET>
# JWT secret: Supabase dashboard → Settings → API → JWT Secret
VITE_BEN_SUPABASE_ANON_KEY=
